package constant

// StatusClientClosedRequest is a non-standard status code (used by nginx) that indicates
// the client closed the connection before the server could return a response.
const StatusClientClosedRequest = 499
